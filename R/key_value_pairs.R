# key_value_pairs.R

types = c("Estimated Net Flow", "Total Net Assets", "Total Net Assets", "Estimated Net Flow")

swap.type.names <- c("tips.flow.", "tips.assets.")
names(swap.type.names) <- c("Estimated Net Flow", "Total Net Assets")
series.types <- swap.type.names[types]
